apiVersion: v1
kind: Service
metadata:
  name: nguti-app-service
spec:
  selector:
    app: nguti-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30018
  type: NodePort
